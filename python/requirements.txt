# Зависимости демона навигации (nav_daemon.service -> python/voice_nav_daemon.py).
#
# Раньше файл был сырым `pip freeze` со всей системы платы (300+ пакетов ОС,
# включая apt-only python-apt/PyGObject/dbus-python/unattended-upgrades) —
# `pip install -r requirements.txt` в чистом окружении падал на них. Здесь
# оставлены только реально импортируемые в коде проекта пакеты.

requests==2.33.1          # HTTP-запросы к локальному OSRM-серверу (127.0.0.1:5000)
vosk==0.3.45               # офлайн-распознавание речи (STT)

# gps (python-gps, клиент gpsd) на большинстве плат ставится через apt, а не
# pip: `apt install python3-gps`. Если pip-пакета "gps" на вашей плате нет —
# ставьте так, import gps будет резолвиться в системный site-packages.
gps==3.22

# Лёгкий inference-only пакет для запуска .rknn на самой плате из Python
# (пригодится для точечной проверки NPU без пересборки C++ ядра). Вендорен
# в репозиторий вместо внешнего /root/diplom/... — см. third_party/rknn/README.md.
# Собран под Python 3.10 (cp310); если на плате другая версия — см. README там.
file:///root/diplom-cpp/third_party/rknn/python/rknn_toolkit_lite2-2.3.2-cp310-cp310-manylinux_2_17_aarch64.manylinux2014_aarch64.whl
