#pragma once

#include <string>
#include <vector>

// Фразы о состоянии самого устройства — не о препятствиях.
//
// Отдельный файл, а не строковый литерал по месту, ровно по той же причине, по
// какой отдельно живут названия классов: тот же текст нужен
// tools/build_voice_cache.sh, который синтезирует реплики заранее. Ключ кэша
// считается от текста фразы, поэтому расхождение в одну букву означает промах
// мимо кэша — и сообщение об отказе камеры человек услышит через пять секунд
// после самого отказа, ровно тогда, когда эти секунды и дороги.
extern const char* PHRASE_CAMERA_BLIND;
extern const char* PHRASE_CAMERA_BACK;

// Всё, что произносится о состоянии устройства — для предварительного синтеза.
std::vector<std::string> device_phrases();
