# Метрики модели BlindNav

Как измерялось, что получилось и что из этого следует. Все числа получены
14.08.2026 на датасете `DATASET/` (10 классов, 33 457 train / 1997 val,
разметка полигонами) и на самой плате Orange Pi 3B (RK3566).

Инструменты воспроизведения — в [tools/eval/](tools/eval/), см. раздел
«Как повторить» в конце.

## 1. Главное в двух строках

- Перенос модели на NPU **ничего не стоит по точности**: FP16 на плате даёт
  mAP50 0.2061 против 0.2059 у исходного ONNX на PC.
- Общий mAP низкий (0.197), но он не описывает работу устройства: в зоне
  предупреждений (ближе 7 м) при боевом пороге модель находит **91.5% машин и
  65.8% людей** при точности 0.90 и 0.93 соответственно.

## 2. Качество модели (весь val, 1997 изображений)

`weights/best.onnx`, imgsz 512, стандартная валидация ultralytics:

| Метрика | Значение |
| :--- | ---: |
| mAP50 | 0.197 |
| mAP50-95 | 0.107 |
| precision | 0.454 |
| recall | 0.209 |

По классам (AP50): car 0.436, traffic_light 0.321, traffic_sign 0.216,
person 0.223, curb 0.192, crosswalk 0.188, sidewalk 0.143, pole 0.116,
trash_can 0.108, bench 0.030.

Собственный пайплайн метрик ([tools/eval/bn_eval.py](tools/eval/bn_eval.py))
на подвыборке 500 изображений даёт mAP50 0.2059 — сходится с ultralytics
(0.197 на полном наборе), значит числам ниже можно верить.

## 3. Сколько стоит перенос на NPU

Одна и та же подвыборка (500 изображений val), один и тот же decode/NMS:

| Вариант | Где | mAP50 | mAP50-95 |
| :--- | :--- | ---: | ---: |
| `best.onnx` (FP32) | PC, onnxruntime | 0.2059 | 0.1132 |
| `yolo11_final.rknn` (FP16) | плата, NPU | **0.2061** | **0.1131** |

**Вывод: потерь нет.** Расхождение в третьем знаке — шум выборки, а не
деградация. Искать точность в конвертации модели или в NPU бессмысленно.

### Препроцессинг тоже не виноват

Боевой `main.cpp` сжимает кадр в 512×512 без сохранения пропорций
(`cv::resize`), тогда как обучение шло с letterbox. Проверено на той же
подвыборке: squash 0.2059, letterbox 0.2021. Разницы нет — переделывать
препроцессинг не нужно.

## 4. Что видит пользователь: метрика в зоне предупреждений

mAP считается по всем размеченным объектам, включая столб на горизонте
высотой в десяток пикселей. Устройство же предупреждает только об объектах
ближе `MAX_DANGER_DIST = 7 м`, а дистанция берётся из высоты бокса
(`compute_distance()` в `threat_logic.cpp`). Если применить то же правило к
разметке и предсказаниям, картина другая — весь val (1997 изображений), порог
0.30, тот самый `CONF_THRESHOLD` из `main.cpp`:

| Класс | precision | recall | объектов в зоне |
| :--- | ---: | ---: | ---: |
| car | 0.886 | **0.911** | 716 |
| person | **0.876** | **0.734** | 154 |
| traffic_sign | 0.719 | 0.607 | 211 |
| pole | 0.646 | 0.537 | 659 |
| traffic_light | 0.732 | 0.529 | 155 |
| curb | 0.661 | 0.440 | 3823 |
| trash_can | 0.420 | 0.304 | 112 |
| crosswalk | 0.721 | 0.263 | 1542 |
| sidewalk | 0.650 | 0.170 | 7457 |
| bench | 0.400 | 0.118 | 17 |

Для сравнения, те же классы **без** фильтра по дистанции при том же пороге:
person P 0.718 / R 0.142, car P 0.821 / R 0.397, pole P 0.620 / R 0.093.
Разрыв объясняет всё: людей в разметке 1636, а в опасной зоне — 154; столбов
9226 против 659. Низкий mAP набирается на далёкой мелочи, до которой
устройству нет дела.

Снижение порога до 0.10 поднимает полноту по людям 0.734 → 0.786, но роняет
точность 0.876 → 0.729 (каждое четвёртое предупреждение ложное). Текущий
порог 0.30 выбран разумно.

### Развёртка порога по классам

Предположение, что отдельный порог на класс даст заметную прибавку, проверено
и по большей части не подтвердилось. `tools/eval/run_thresh_sweep_rknn.py`,
INT8-модель на NPU, 500 кадров val, учитываются только объекты зоны < 7 м
(в ячейке точность/полнота):

| класс | 0.10 | 0.15 | 0.25 | 0.30 | n_gt |
| :--- | :--- | :--- | :--- | :--- | ---: |
| person | 0.72/0.68 | 0.72/0.68 | 0.86/0.61 | 0.89/0.61 | 41 |
| car | 0.82/0.92 | 0.84/0.91 | 0.90/0.89 | 0.91/0.89 | 212 |
| curb | 0.27/0.64 | 0.38/0.55 | 0.49/0.49 | 0.65/0.38 | 985 |
| pole | 0.28/0.73 | 0.34/0.68 | 0.45/0.57 | 0.60/0.49 | 173 |
| traffic_sign | 0.51/0.76 | 0.62/0.76 | 0.71/0.72 | 0.86/0.70 | 54 |
| **traffic_light** | 0.42/0.67 | 0.50/0.62 | **0.57/0.62** | 0.55/0.40 | 42 |
| trash_can | 0.21/0.45 | 0.27/0.41 | 0.35/0.38 | 0.47/0.28 | 29 |

Прежнее предположение про `pole` — «полнота 0.728 при 0.10, а ложный столб
дёшев» — измерения не выдержало: при 0.10 точность падает до 0.28, то есть
почти три предупреждения о столбе из четырёх ложные. Это не дешёвая ошибка,
а устройство, которое перестают слушать. То же у `curb` и `trash_can`:
снижение порога просто меняет пропуски на ложные срабатывания примерно один
к одному.

Единственное исключение — `traffic_light`: при 0.25 растёт и полнота
(0.40 → 0.62), и точность (0.55 → 0.57). Это не размен, а строгое улучшение,
поэтому в `main.cpp` для него выставлен отдельный порог. Оговорка: в зоне
всего 42 размеченных светофора, разница в полноте держится примерно на двух
стандартных ошибках — направление достоверно, величина нет.

Замечание к методике: развёртка декодирует с порогом 0.05 и применяет NMS
до отсечения по порогу, поэтому колонка 0.30 не в точности повторяет прежнее
поведение устройства, где отсечение шло до NMS. В `main.cpp` порядок приведён
к тому же виду, что в замере. NMS в замере считается по IoU 0.7, в прошивке —
0.45; на выбор порога это не влияет, но абсолютные числа между таблицами
сравнивать нельзя.

Отдельно: `sidewalk` и `crosswalk` размечены полигонами площадей, а не
объектами. Их «высота бокса» не имеет физического смысла, поэтому и оценка
дистанции для них бессмысленна — низкая полнота в таблице выше во многом
артефакт постановки, а не ошибка модели.

## 5. Скорость: где на самом деле уходит время

### Модель не INT8, а FP16

Опрос загруженной модели через `rknn_query` (`RKNN_QUERY_NATIVE_NHWC_INPUT_ATTR`
и `RKNN_QUERY_OUTPUT_ATTR`) показывает:

```
INPUT : dims=[1,512,512,3] type=FP16 fmt=NHWC size_with_stride=1572864
OUT[0]: dims=[1,74,64,64]  type=FP16 fmt=NCHW zp=0 scale=1.000000
OUT[1]: dims=[1,74,32,32]  type=FP16 ...
OUT[2]: dims=[1,74,16,16]  type=FP16 ...
```

`scale=1.0`, `zp=0`, тип FP16 — квантования нет. Утверждение README про
«YOLOv11-Nano (INT8)» не соответствует тому, что реально лежит в
`blind_nav/model/yolo11_final.rknn`.

Отсюда объяснение старой загадки: 27% времени кадра в `rknn_inputs_set` — это
не лишнее копирование 786 КБ, а конверсия uint8 → fp16 на 786 432 значения,
которую рантайм обязан сделать.

### Zero-copy вход: проверено, не помогает

Реализован zero-copy (`rknn_create_mem` + `rknn_set_io_mem`, конверсия в fp16
силами OpenCV прямо в буфер NPU) с автоматическим откатом на обычный путь.
A/B на одном бинарнике (`BLIND_NAV_NO_ZERO_COPY=1/0`), 80 кадров, плата
прогрета, два прогона на вариант:

| Стадия кадра | обычный путь | zero-copy |
| :--- | ---: | ---: |
| resize + cvtColor | 5.8 мс | 7.4 мс |
| передача входа | 25.6 мс | **0.24 мс** |
| `rknn_run` | 56.1 мс | **87.9 мс** |
| **Средний FPS** | **10.56 / 10.58** | 9.75 / 9.75 |

Копирование исчезло, но ровно та же работа всплыла внутри `rknn_run`:
конверсию в формат, который нужен NPU, всё равно кто-то делает. Детекции
идентичны (303 объекта, те же классы и уверенности), то есть путь корректен —
просто бесполезен на FP16-модели. Прежняя оценка «zero-copy даст ~14 FPS»
была ошибочной; здесь она опровергнута замером.

Код zero-copy оставлен в `rknn_inference.cpp` выключенным по умолчанию.

### INT8: 2.4x к скорости

Модель пересобрана из `best.onnx` в INT8 прямо на плате
([tools/eval/convert_int8.py](tools/eval/convert_int8.py), калибровка на 300
случайных изображениях train, `asymmetric_quantized-8`). Полный
`rknn-toolkit2` 2.3.2 существует под aarch64, отдельная x86-машина не нужна.

Тот же `rknn_video_bench`, то же видео, та же плата:

| Стадия кадра | FP16 (сейчас) | INT8 |
| :--- | ---: | ---: |
| resize + cvtColor | 5.8 мс | 6.1 мс |
| передача входа | 25.6 мс | **0.6 мс** |
| `rknn_run` | 56.1 мс | **26.0 мс** |
| **Средний FPS** | **10.56** | **25.32 / 25.55** |

Конверсия входа схлопнулась (uint8 → int8 почти бесплатен), сам инференс
ускорился вдвое. Заодно это делает zero-copy окончательно ненужным: передавать
стало нечего. Размер модели 6.7 МБ → 4.0 МБ.

Атрибуты новой модели (`rknn_query`): вход `INT8 zp=-128 scale=0.003922`
(то есть принимает сырой uint8-кадр, как и раньше), выходы `INT8` с
собственными zp/scale — для C++ ничего не меняется, `rknn_outputs_get` с
`want_float=1` деквантует их сам. Три выхода те же (74×64×64 / 74×32×32 /
74×16×16), поэтому `decode.cpp` трогать не нужно.

### Чего стоит INT8 по точности

Та же подвыборка (500 изображений), тот же decode/NMS:

| Модель | mAP50 | mAP50-95 | FPS |
| :--- | ---: | ---: | ---: |
| `yolo11_final.rknn` (FP16) | 0.2061 | 0.1131 | 10.56 |
| `yolo11_int8.rknn` | 0.1725 | 0.0900 | **25.32** |

В зоне предупреждений (< 7 м, порог 0.30) — то, что видит пользователь:

| Класс | FP16 P / R | INT8 P / R |
| :--- | ---: | ---: |
| car | 0.898 / 0.915 | 0.909 / 0.891 |
| person | 0.931 / 0.658 | 0.893 / 0.610 |
| traffic_sign | 0.826 / 0.704 | 0.864 / 0.704 |
| pole | 0.640 / 0.567 | 0.600 / 0.485 |
| traffic_light | 0.750 / 0.571 | 0.548 / 0.405 |
| curb | 0.663 / 0.430 | 0.647 / 0.383 |

Итог сделки: **2.4x скорости за 5% полноты по людям и 2% по машинам**.
Заметнее всего страдает светофор (полнота 0.571 → 0.405) — цветовой признак
чувствителен к квантованию.

Побочный эффект: у INT8 уверенности квантованы (выход int8 со `scale≈1.02` на
логитах), поэтому значения ложатся на дискретную сетку — на эталонном фото
видно повторяющиеся `conf=0.734881` и ровное `conf=0.5`. Порог 0.30 при этом
работает грубее, чем на FP16: соседние уровни отстоят друг от друга заметно.

### Что стоит на плате

С 15.08.2026 боевой `main.cpp` грузит `yolo11_int8.rknn` (константа
`MODEL_PATH`). Прежняя `yolo11_final.rknn` лежит рядом — чтобы откатиться,
достаточно поменять путь в `MODEL_PATH` и пересобрать, формат выходов
одинаковый. Живой прогон с камерой: **540 кадров за 30 с** (инференс 41 мс)
против 355 кадров за 45 с (101 мс) на FP16.

## 6. Диагноз модели: недообучение, а не потолок данных

Ключевой замер — та же метрика на **обучающей** выборке (500 неаугментированных
изображений train, `tools/eval/run_eval_onnx.py` с `train_sub_list.txt`):

| | train | val |
| :--- | ---: | ---: |
| mAP50 | 0.2304 | 0.2059 |
| mAP50-95 | 0.1302 | 0.1132 |
| person AP50 | 0.2075 | 0.2058 |
| car AP50 | 0.4585 | 0.4653 |

Разрыва между train и val нет: модель работает на данных, которые видела при
обучении, так же, как на новых. Это недообучение — сеть не запомнила даже
train. Следствие: потолок данных не достигнут, переобучение имеет смысл,
но борьба с переобучением (аугментации, регуляризация, больше данных «вообще»)
здесь ни при чём.

### Половина разметки физически недетектируема

Распределение высот размеченных объектов, пересчитанное на вход 512×512
(300 изображений val):

| Класс | медиана высоты | доля < 16 px |
| :--- | ---: | ---: |
| person | 16.1 px | 49% |
| car | 13.5 px | 55% |
| pole | 16.1 px | 50% |
| traffic_sign | 8.3 px | 78% |
| curb | 4.8 px | 75% |
| sidewalk | 5.6 px | 75% |

При минимальном страйде 8 объект высотой 16 px — это две ячейки сетки. Такие
цели не берёт nano-модель на 512 в принципе, и именно они формируют низкий
mAP. Это ещё один аргумент оценивать систему по метрике зоны < 7 м.

### Домен данных не совпадает с задачей

Визуальный просмотр выборки: датасет — это **дашкам-съёмка из едущих
автомобилей** (виден капот и дворники), города Японии, Вьетнама, Северной
Америки, Балкан. Устройство же — камера на груди пешехода, скорость 5 км/ч,
тротуар, Санкт-Петербург, другая погода и вывески. Разрыв доменов не
закрывается ни числом эпох, ни размером модели — только съёмкой собственных
данных в целевых условиях.

### Дефект разметки

467 из 33 457 файлов разметки train (1.4%) содержат литеральное `/n` вместо
перевода строки — координаты последнего объекта склеены с началом следующей
строки. Все они из внешней kaggle-части датасета (`kaggle_*`). В val таких нет.
Ultralytics подобные файлы при обучении молча пропускает, то есть эти
изображения в обучении не участвовали. Чинится однократным `sed`.

## 7. Что делать дальше

**Скорость.**

1. Перейти на INT8 — единственный крупный резерв, 10.5 → 25.3 FPS, C++ менять
   не нужно (те же три выхода, тот же `decode()`). Решение за владельцем
   проекта: цена в точности измерена и приведена выше.
2. После INT8 узким местом становится CPU-препроцессинг (6.1 мс, 15% кадра):
   перекрытие с `rknn_run` в два потока или аппаратный RGA дадут ещё ~15%.
3. Zero-copy — тупик, проверено замером (см. выше).

**Точность.** Главный вывод: mAP по всему датасету — плохая метрика для этого
устройства, ориентироваться нужно на полноту в зоне < 7 м. Что реально может
её поднять:

1. **Переобучение с входом 640 вместо 512.** На INT8 это будет ~17 FPS —
   всё ещё в 1.6 раза быстрее сегодняшних 10.5, а мелкие объекты (столбы,
   бордюры, светофоры — самые слабые классы) обычно выигрывают от разрешения
   больше всего.
2. **Убрать `sidewalk` и `crosswalk` из задачи детекции.** Это полигоны
   площадей, а не объекты: их bbox покрывает пол-кадра, физического смысла в
   «дистанции до тротуара по высоте бокса» нет. При этом на них уходит
   ёмкость модели — в валидации 11 815 объектов `sidewalk` против 7 107
   `person`. Им место в семантической сегментации либо в отдельной голове.
3. **Пороги по классам вместо общего 0.30.** У `pole` при пороге 0.10 полнота
   0.537 → 0.728, а ложное предупреждение о столбе стоит дешевле, чем о
   человеке. Для `person` порог 0.30 оставить (точность 0.876).
4. **Класс `bench` (AP50 0.030, 17 объектов в зоне на весь val) —** либо
   набрать данные, либо убрать: сейчас он не работает.

## 8. Как повторить

```bash
# 1. Экспорт трёх "сырых" голов из best.onnx (то, что ждёт decode.cpp)
python tools/eval/make_3head.py weights/best.onnx best_3head.onnx

# 2. Метрики на PC (эталон)
python tools/eval/run_eval_onnx.py best_3head.onnx val_list.txt <корень датасета> squash onnx.json

# 3. Метрики на плате (реальный NPU)
python3 tools/eval/run_eval_rknn.py blind_nav/model/yolo11_final.rknn \
        val_list.txt <корень датасета> squash rknn.json

# 4. Метрика в зоне предупреждений (< 7 м)
python tools/eval/run_nearfield.py best_3head.onnx val_list.txt <корень датасета> near.json
```

`val_list.txt` — список путей вида `images/val/<имя>.jpg` относительно корня
датасета. Разметка ищется рядом, в `labels/val/`.
